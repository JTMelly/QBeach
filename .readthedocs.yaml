version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .  # Installs your own package so autodoc can inspect it

sphinx:
  configuration: docs/source/conf.py